Plik Praca.py należy uruchomić jako administrator
W pliku Praca.py jest zmienna BROKER gdzie trzeba podać adres IP serwera MQTT, bez tego program się nie uruchomi

TagTypeEnum.py to klasa enum,żeby była możliwość określenia jakim typem konkretnie jest tag

W folderze ela->bluetooth->adverising są klasy gdzie ramke danych parsuje sie na okrelone wartosci np temperature,rht
oraz jest klasa factory i base do których odwołuje się plik Praca.py 

